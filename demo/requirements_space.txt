# HuggingFace Spaces requirements for Audio Attribute Studio.
# Pin versions to match the dev environment (requirements_1.txt).
# torch / torchaudio must be installed via the Spaces GPU image;
# list the remaining packages here.

gradio==5.24.0
librosa==0.10.2.post1
matplotlib==3.10.1
numpy>=1.24,<2.0
scipy==1.15.2
soundfile==0.13.1
safetensors>=0.4.0
